# 🤖 Claude Code Integration Setup - VS Code

## ✅ **EXTENSIONS INSTALLED**
- **Claude Code for VS Code** ✅ Already installed
- **Cline** (Claude Dev) ✅ Ready to install

## 🔧 **SETUP CLAUDE IN VS CODE**

### 1. **Configure Claude Code Extension**
1. Open VS Code Command Palette (`Ctrl+Shift+P`)
2. Type "Claude: Set API Key"
3. Get your API key from https://console.anthropic.com/ and paste it

### 2. **Configure Cline (Claude Dev)**
1. Press `Ctrl+Shift+P`
2. Type "Cline: Setup"
3. Choose "Anthropic Claude"
4. Enter your API key when prompted
5. Select model: `claude-3-haiku-20240307` (fast and cost-effective)

## 💬 **HOW TO USE CLAUDE IN VS CODE**

### **Method 1: Claude Code Chat**
1. Open Command Palette (`Ctrl+Shift+P`)
2. Type "Claude: Start Chat"
3. Ask Claude to help with your code:
   - "Audit my Boston Drone School codebase for Render deployment"
   - "Fix my TypeScript compilation errors"
   - "Optimize my API routes"
   - "Review my render.yaml configuration"

### **Method 2: Cline Agent**
1. Open Command Palette (`Ctrl+Shift+P`)
2. Type "Cline: Start"
3. Give Claude tasks like:
   - "Review all files and fix Render deployment issues"
   - "Update all Anthropic SDK calls to use correct models"
   - "Add missing environment variables"
   - "Optimize database connections for production"

### **Method 3: Inline Code Help**
1. Select code in any file
2. Right-click → "Claude: Explain Code"
3. Or use "Claude: Fix Code" to get suggestions

## 🎯 **IMMEDIATE TASKS FOR CLAUDE**

Ask Claude to help with these specific issues:

### **Deployment Fixes**
```
Claude, please review my Boston Drone School codebase and:
1. Fix all Anthropic SDK model references to use working models
2. Ensure render.yaml is properly configured  
3. Add missing environment variables
4. Fix TypeScript build issues
5. Optimize database connections
6. Add proper error handling
```

### **Code Quality**
```
Claude, audit my codebase and:
1. Check for security vulnerabilities
2. Optimize performance bottlenecks
3. Ensure production readiness
4. Fix any deployment blockers
5. Validate all API endpoints
```

## ⚡ **QUICK START COMMANDS**

### In VS Code:
- `Ctrl+Shift+P` → "Claude: Start Chat"
- `Ctrl+Shift+P` → "Cline: Start"  
- Select code → Right-click → "Claude: Explain Code"

### Example Prompts:
- "Fix my Render deployment configuration"
- "Audit this API route for security issues"
- "Optimize this database query"
- "Add proper error handling to this function"

## 🔥 **POWER USER TIPS**

1. **Multi-file Analysis**: Claude can analyze your entire codebase
2. **Automatic Fixes**: Cline can make file changes automatically
3. **Context Aware**: Claude understands your project structure
4. **Deploy Ready**: Ask Claude to make your code Render-ready

---

**You now have Claude integrated directly in VS Code!** 
**Use `Ctrl+Shift+P` and type "Claude" to get started!** 🚀